---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: externalprocessorattachments.customrouter.freepik.com
spec:
  group: customrouter.freepik.com
  names:
    kind: ExternalProcessorAttachment
    listKind: ExternalProcessorAttachmentList
    plural: externalprocessorattachments
    singular: externalprocessorattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Gateway selector
      jsonPath: .spec.gatewayRef.selector
      name: Gateway
      type: string
    - description: External processor service
      jsonPath: .spec.externalProcessorRef.service.name
      name: Processor
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ExternalProcessorAttachment is the Schema for the externalprocessorattachments API.
          It attaches an external processor to a Gateway by generating the necessary EnvoyFilters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ExternalProcessorAttachment
            properties:
              catchAllRoute:
                description: |-
                  catchAllRoute configures automatic generation of a catch-all route.
                  When specified, the operator generates an EnvoyFilter that creates a default route
                  for the specified hostnames, allowing CustomHTTPRoute to handle requests
                  without requiring a base HTTPRoute to be configured separately.
                properties:
                  backendRef:
                    description: |-
                      backendRef defines the default backend service to route unmatched requests to.
                      This is used when no CustomHTTPRoute matches the request.
                    properties:
                      name:
                        description: 'name is the name of the Service (must be a valid
                          RFC 1123 label: lowercase alphanumeric or ''-'')'
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      namespace:
                        description: namespace is the namespace of the Service
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      port:
                        description: port is the port of the Service
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - name
                    - namespace
                    - port
                    type: object
                  hostnames:
                    description: |-
                      hostnames is a list of hostnames that the catch-all route should match.
                      When specified, the catch-all route will only be generated for these hostnames.
                      If empty, no catch-all route will be generated.
                    items:
                      type: string
                    minItems: 1
                    type: array
                required:
                - backendRef
                - hostnames
                type: object
              externalProcessorRef:
                description: externalProcessorRef identifies the external processor
                  service to use
                properties:
                  messageTimeout:
                    default: 5s
                    description: |-
                      messageTimeout is the timeout for individual messages sent to the external processor.
                      This applies to each request/response exchange with the ext_proc service.
                      Must be a valid duration string (e.g., "1s", "500ms", "5s").
                      Defaults to "5s" if not specified.
                    type: string
                  service:
                    description: service is the reference to the external processor
                      Kubernetes Service
                    properties:
                      name:
                        description: name is the name of the Service
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the namespace of the Service
                        minLength: 1
                        type: string
                      port:
                        description: port is the port of the Service
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - name
                    - namespace
                    - port
                    type: object
                  timeout:
                    default: 5s
                    description: |-
                      timeout is the gRPC timeout for the external processor service.
                      This is the timeout for establishing the gRPC connection.
                      Must be a valid duration string (e.g., "1s", "500ms", "2s").
                      Defaults to "5s" if not specified.
                    type: string
                required:
                - service
                type: object
              gatewayRef:
                description: gatewayRef identifies the Gateway workload to attach
                  the external processor to
                properties:
                  selector:
                    additionalProperties:
                      type: string
                    description: |-
                      selector is a set of labels used to identify the Gateway workload.
                      These labels are used in the EnvoyFilter's workloadSelector.
                    minProperties: 1
                    type: object
                required:
                - selector
                type: object
            required:
            - externalProcessorRef
            - gatewayRef
            type: object
          status:
            description: status defines the observed state of ExternalProcessorAttachment
            properties:
              conditions:
                description: conditions represent the current state of the ExternalProcessorAttachment
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
