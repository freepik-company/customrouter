name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Run on Default
    runs-on: default
    steps:
      - name: Clone the code
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Running Tests
        run: |
          go mod tidy
          make test
